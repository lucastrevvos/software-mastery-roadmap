name: Upgrade Amaris Interview Copilot V3

on:
  push:
    paths:
      - 'tools/upgrade_amaris_v3.py'
      - '.github/workflows/upgrade-amaris-v3.yml'

permissions:
  contents: write

jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build V3
        run: python tools/upgrade_amaris_v3.py
      - name: Commit V3
        run: |
          if git diff --quiet -- docs/amaris-fullstack-interview.html; then
            echo "No changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/amaris-fullstack-interview.html
          git commit -m "feat: add Alexandre Mode and richer interview answers"
          git push
