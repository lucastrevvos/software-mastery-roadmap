name: Upgrade Amaris Interview Copilot V4.3

on:
  push:
    paths:
      - 'tools/upgrade_amaris_v43.py'
      - '.github/workflows/upgrade-amaris-v43.yml'

permissions:
  contents: write

jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build V4.3
        run: python tools/upgrade_amaris_v43.py
      - name: Commit V4.3
        run: |
          if git diff --quiet -- docs/amaris-fullstack-interview.html; then
            echo "No changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/amaris-fullstack-interview.html
          git commit -m "fix: make Node concurrency question searchable"
          git push
