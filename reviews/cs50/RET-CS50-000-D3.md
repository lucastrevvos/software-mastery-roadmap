# RET-CS50-000-D3 — Retenção direcionada

**Origem:** `RET-CS50-000-D1` com retenção parcial  
**Data prevista:** 2026-08-24  
**Modo:** Learning Mode  
**Tempo alvo:** 15–20 min

## Regra

Faça sem abrir as respostas anteriores. Esta rodada é curta e direcionada aos vazamentos que reapareceram, com pequenos spot-checks dos conceitos que já sobreviveram ao intervalo.

Crie `reviews/cs50/answers-RET-CS50-000-D3.md`.

## A — Estado imediatamente após cada linha

Execute exatamente:

```text
value = 4
value = value + 3
value = value * 2
value = value - 5
```

Informe `value` imediatamente após cada uma das quatro linhas.

Depois explique em uma frase por que o valor de uma linha futura não pode ser usado para responder ao estado de uma linha anterior.

## B — Loop e condição de parada

Considere:

```text
count = 0
while count < 4
    count = count + 1
```

Responda:

1. Qual é a condição de continuação do loop?
2. Qual instrução faz o estado avançar em direção à parada?
3. Qual é o valor final de `count`?
4. O que poderia acontecer se `count = count + 1` fosse removido?

Depois escreva, em uma frase, um modelo geral para um loop que **não deve ser infinito**.

## C — Evento × ação

Num aplicativo:

```text
EVENTO: usuário pressiona "Salvar"
AÇÃO: validar dados e persistir o formulário
```

Explique a diferença entre o evento e a ação sem usar a palavra “mesma coisa” nem definir um pelo outro.

## D — Transferência com estado completo

Estado inicial:

```text
processed = 0
errors = 0
mode = "running"
```

Regras:

- sucesso: `processed = processed + 1`;
- falha: `errors = errors + 1`;
- se `errors >= 2`: `mode = "stopped"`.

Eventos, na ordem:

```text
sucesso
falha
sucesso
falha
```

Informe o **estado final completo**:

```text
processed =
errors =
mode =
```

E diga qual evento fez `mode` mudar.

## E — Spot-check de fluxo aninhado

```text
x = 3
enabled = false

if enabled
    x = x + 5
    if x > 4
        status = "ON"
```

Responda:

```text
x final =
a condição x > 4 foi avaliada? =
status recebeu "ON"? =
```

## F — Spot-check de restart

Estado inicial:

```text
score = 0
lives = 2
mode = "playing"
```

Estado atual:

```text
score = 7
lives = 1
mode = "paused"
```

Informe o estado completo após **restart completo**.

## Entrega

```text
git add .
git commit -m "RET-CS50-000-D3 enviado"
git push
```

No chat:

`RET-CS50-000-D3 enviado`
