# RET-CS50-001-D1 — Retenção D+1 da Week 1 / C

**Origem:** CS50 Week 1 — C + LAB-CS50-001  
**Data prevista:** 2026-08-22  
**Modo:** Learning Mode  
**Tempo alvo:** 15–20 min

## Regra

Faça sem abrir `answers.md`, `answers-r1.md`, `answers-r2.md`, `REVIEW-01.md` ou `REVIEW-02.md`. A finalidade é medir o que permaneceu disponível depois do intervalo.

Crie `reviews/cs50/answers-RET-CS50-001-D1.md`.

## A — Recall

Responda em 1–2 frases cada:

1. Qual é a diferença entre `make` e um compilador como `gcc`/`clang`?
2. Por que `double result = 7 / 2;` não produz `3.5`?
3. Qual é a diferença entre conversão implícita e cast explícito?
4. O que é integer overflow?
5. Por que representar dinheiro internamente em centavos inteiros pode ser melhor que usar `float`?

## B — Trace

Sem executar primeiro:

```c
int x = 4;

for (int i = 0; i < 3; i++)
{
    if (i % 2 == 0)
    {
        x = x + 2;
    }
    else
    {
        x = x - 1;
    }
}
```

Registre o valor de `x` após cada iteração e o valor final.

## C — Tipos e divisão

Preveja os resultados e explique o ponto exato em que há ou não perda de informação:

```c
int a = 9;
int b = 4;
double r1 = a / b;
double r2 = (double) a / b;
double r3 = a / 4.0;
```

Informe `r1`, `r2` e `r3`.

## D — Conversões

Considere:

```c
double price = 27.89;
int whole1 = price;
int whole2 = (int) price;
```

Responda:

- qual conversão é implícita;
- qual é explícita;
- qual valor chega em `whole1` e `whole2`;
- se escrever o cast explícito garante que qualquer valor original caberá corretamente em `int`.

## E — Repair

Encontre o bug conceitual sem executar primeiro:

```c
int total = 15;
int people = 4;
double average = total / people;
printf("%.2f\n", average);
```

Explique o comportamento real e faça uma correção mínima que preserve `3.75`.

## F — Verificação e transferência

Um programa recebe um inteiro positivo e soma seus dígitos usando `% 10` e `/ 10`.

1. Escreva três casos de teste úteis, incluindo um caso de borda ou validação.
2. Para representar `R$ 123,45` sem ponto flutuante internamente, informe o valor inteiro armazenado em centavos e como recuperar reais e centavos.

## Entrega

```bash
git add .
git commit -m "RET-CS50-001-D1 enviado"
git push
```

No chat: `RET-CS50-001-D1 enviado`
